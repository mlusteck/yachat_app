var newMessages = "<%= j render partial: 'rooms/messages' %>";
if( newMessages == "") {
  getMoreMessages = function () {};
}
else {
  $("#messages").prependWithoutScroll(newMessages);
  continueGettingMessages();
  if ($('#message-top').isInViewport()) {
    getMoreMessages();
  }
}
